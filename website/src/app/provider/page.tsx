'use client'; import {useEffect,useState} from 'react'; const API=process.env.NEXT_PUBLIC_API||"http://localhost:8000"; export default function Page(){const [data,setData]=useState<any>(null); useEffect(()=>{const t=localStorage.getItem('access')||''; fetch(`${API}/api/kpi/overview/`,{headers:{Authorization:`Bearer ${t}`}}).then(r=>r.json()).then(setData).catch(()=>setData({error:true}));},[]); return(<main dir="rtl" className="container py-6"><h1 className="text-xl font-bold mb-4">provider</h1><div className="card"><pre>{JSON.stringify(data,null,2)}</pre></div></main>);}
